"""Decorative ground-cover props (D-100).

Separate from `art/units` and `art/buildings` because these are neither: a
grass tuft is not a simulation entity at all. Nothing in this package is
referenced by a `.tres`, nothing goes on the wire, and a missing prop costs
the picture some texture rather than costing the game a rule.
"""
